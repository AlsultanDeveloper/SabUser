// This is a placeholder screen for the SAB floating button
// It doesn't render anything - the actual button is in _layout.tsx
import { View } from 'react-native';

export default function SabPlaceholder() {
  return <View />;
}
