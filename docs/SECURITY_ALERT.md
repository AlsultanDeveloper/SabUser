# 🚨 تنبيه أمني عاجل - مطلوب إجراء فوري

## الملخص
اكتشف GitHub مفاتيح API مكشوفة في مستودعك. تم رفع عدة مفاتيح Firebase و Google API إلى تاريخ git، مما يجعلها متاحة للعامة.

## الإجراءات الفورية المطلوبة

### 1. إلغاء جميع مفاتيح API المكشوفة

**مفاتيح مشروع Firebase (الأولوية العالية):**
- المشروع الحالي: `sab-store-9b947`
- اذهب إلى [وحدة تحكم Firebase](https://console.firebase.google.com/)
- اختر المشروع `sab-store-9b947`
- اذهب إلى إعدادات المشروع > عام > تطبيقاتك
- أعد إنشاء جميع مفاتيح تكوين Firebase

**مشروع Firebase المكشوف (من check-firebase-products.js):**
- معرف المشروع القديم: `sabuser-25569`
- مفتاح API: `AIzaSyAAl3WvONnGdkcN8VxqLrPBKQV6poxQQeQ`
- إذا كان لديك وصول، قم بتعطيل هذا المشروع فوراً

**مفتاح Google Maps API:**
- المفتاح: `AIzaSyDMt-RGeUT1wtUDepRCRoGu0Q05I4RD-2o`
- اذهب إلى [وحدة تحكم Google Cloud](https://console.cloud.google.com/)
- انتقل إلى APIs & Services > Credentials
- ابحث عن المفتاح واحذفه/أعد إنشاءه

**معرفات عميل Google OAuth:**
- جميع معرفات عميل OAuth في ملف .env مكشوفة
- أعد إنشاءها في وحدة تحكم Google Cloud

### 2. تنظيف تاريخ Git

تم رفع ملف .env عدة مرات. تحتاج لحذفه من تاريخ git:

```bash
# إزالة .env من تاريخ git (مدمر - اعمل نسخة احتياطية أولاً!)
git filter-branch --force --index-filter \
'git rm --cached --ignore-unmatch .env' \
--prune-empty --tag-name-filter cat -- --all

# رفع قسري للريموت (تحذير: هذا يعيد كتابة التاريخ)
git push origin --force --all
```

### 3. تحديث التكوين

1. **أعد إنشاء جميع مفاتيح API** في وحدات التحكم المخصصة لها
2. **حدث ملف .env المحلي** بالمفاتيح الجديدة
3. **لا ترفع ملف .env مرة أخرى** (تم إضافته لـ .gitignore)
4. **حدث بيئة الإنتاج** بالمفاتيح الجديدة

### 4. مراقبة الاستخدام المسيء

- تحقق من فوترة Google Cloud للاستخدام غير المتوقع
- راقب مقاييس استخدام Firebase
- إعداد تنبيهات الفوترة
- مراجعة استخدام مفاتيح API في وحدات التحكم

## ما تم إصلاحه

✅ إزالة مفتاح API المرمز من `check-firebase-products.js`
✅ إصلاح مفتاح Google Maps API المرمز في `MapPicker.tsx`
✅ إضافة ملفات .env إلى .gitignore
✅ تثبيت dotenv لتحميل متغيرات البيئة الآمن

## الوقاية

1. **لا ترفع ملفات .env أبداً**
2. **استخدم متغيرات البيئة لجميع الأسرار**
3. **عمليات تدقيق أمني منتظمة**
4. **استخدم أدوات مثل git-secrets لمنع التسريبات المستقبلية**

## تحتاج مساعدة؟

إذا كنت تحتاج مساعدة في أي من هذه الخطوات، يرجى إخباري. هذه مشكلة أمنية خطيرة تتطلب اهتماماً فورياً.

## خطوات مفصلة للحل

### الخطوة 1: إعادة إنشاء مفاتيح Firebase
1. اذهب إلى https://console.firebase.google.com/
2. اختر مشروع `sab-store-9b947`
3. انقر على ⚙️ (الإعدادات) > إعدادات المشروع
4. انتقل إلى تبويب "عام"
5. مرر لأسفل إلى قسم "تطبيقاتك"
6. انقر على التطبيق الويب
7. انسخ التكوين الجديد

### الخطوة 2: إعادة إنشاء مفاتيح Google Maps
1. اذهب إلى https://console.cloud.google.com/
2. اختر المشروع المناسب
3. انتقل إلى "APIs & Services" > "Credentials"
4. ابحث عن مفتاح Google Maps API
5. احذف المفتاح القديم
6. أنشئ مفتاح جديد
7. قم بتقييد المفتاح للنطاقات المناسبة

### الخطوة 3: تحديث ملف .env
```env
# استبدل هذه القيم بالمفاتيح الجديدة
EXPO_PUBLIC_FIREBASE_API_KEY=المفتاح_الجديد_هنا
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=مشروعك.firebaseapp.com
EXPO_PUBLIC_FIREBASE_PROJECT_ID=معرف_مشروعك
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=مشروعك.appspot.com
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=معرف_المرسل
EXPO_PUBLIC_FIREBASE_APP_ID=معرف_التطبيق
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=مفتاح_خرائط_جوجل_الجديد
```